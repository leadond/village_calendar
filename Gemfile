source "https://rubygems.org"

# a0's App Store submission step uses Bundler to run fastlane.
# The previous pin (2.231.1) is not a RubyGems release, which causes:
#   bundler: failed to load command: fastlane
# Pin to a published, recent fastlane version.

gem "fastlane", "2.230.0"
